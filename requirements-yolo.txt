# YOLO 检测训练环境
# 说明：
# - torch/torchvision 建议从 PyTorch cu130 源安装，避免和本地 GPU 栈不一致。
# - 例如：
#   python -m pip install --upgrade --force-reinstall torch==2.10.0 torchvision==0.25.0 --index-url https://download.pytorch.org/whl/cu130
numpy==1.26.4
opencv-python==4.10.0.84
lmdb==1.7.5
torch==2.10.0
torchvision==0.25.0
ultralytics==8.4.46
